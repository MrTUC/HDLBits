/*
 * Here also and secondgate ( out, a, b ); could be used

module top_module ( 
    input a, 
    input b, 
    output out );

    assign out = a & b;
    // and secondgate ( out, a, b );
    
endmodule
